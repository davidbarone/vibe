# Compare-Csv

## Summary
Compare-Csv is a PowerShell script that compares 2 csv files. The code was generated by Copilot, using a spec included here (`Compare-Csv.md`).

## To Regenerate / Make Modifications
To update functionality, the following is recommended:
- Create a new Copilot session.
- Upload the existing `Compare-Csv.ps1` script
- Upload the new specification, `Compare-Csv.md`
- Ask Copilot to review the specification for any inconsistencies and make any changes as required
- Ask Copilot to generate a new script

## What Compare-Csv Does
Compare-Csv compares 2 csv files and outputs differences. The compare tool performs 2 levels of checks
- Schema-Level checks
- Row-Level checks

Firstly, the 2 files are checked to ensure they have identical schemas. Both files require:
- Identical column names
- Identical data types

If the schemas are not identical, the schema check will report this, and the script will above before running a row-by-row compare.

If the schemas match, the comparison will proceed to a row-by-row comparison. There are 3 types of errors that are reported:
- Row (based on key column(s)) exists in the left file but not the right file
- Row (based on key column(s)) exists in the right file but not the left file
- A value a row (based on a key column) contains a different value between left and right files

A report is then output showing row-level errors. The script can be configured to show detailed row-level errors, or summary row-level errors. 

## Running the Compare-Csv Script
The PowerShell example below illustrates how the compare can be invoked:

``` powershell
Compare-Csv `
    -Left 'left.csv' `
    -Right 'right.csv' `
    -KeyColumns 'recordId' `
    -ExcludeColumns 'columnE' `
    -NullValues 'null' `
    -ApplyTrim $true `
    -ScanRows 10 `
    -SummariseResults $false `
    -DetailedRows 500 `
    -AutoDetectTypes $true `
    -RoundRules 'columnA:1mi,columnB:1dp'
```

There is a single main function: `Compare-Csv` which can be called with a variety of parameters to customise behaviour:

| Parameter Name      | Data Type | Mandatory | Default           | Purpose                                                                                                                                                   |
| ------------------- | --------- | --------- | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `$Left`             | `String`  | yes       |                   | The full file path of the first CSV file.                                                                                                                 |
| `$Right`            | `String`  | yes       |                   | The full file path of the second CSV file.                                                                                                                |
| `$KeyColumns`       | `String`  | yes       |                   | If set, should be a comma-separated list of key columns. Used during the COMPARE process.                                                                 |
| `$Delimiter`        | `String`  | no        | ','               | The delimiter used in the csv file. Defaults to a comma character (',').                                                                                  |
| `$RowIdName`        | `String`  | no        | `[string]::Empty` | If set, then an additional integer column of the same name as the rowIdName parameter is created in both the left and right datasets.                     |
| `$ExcludeColumns`   | `String`  | no        | `[string]::Empty` | If set, should be a comma-separated list of columns to exclude from all processing (EXCLUDE LEFT and EXCLUDE RIGHT steps).                                |
| `$NullValues`       | `String`  | no        | `[string]::Empty` | If set, should be a comma-separated list of values to be considered as null (for example 'null,-,(null)')                                                 |
| `$ColumnTypes`      | `String`  | no        | `[string]::Empty` | Optional parameter. If set, is specification of columns. Not all columns need types specified. Example format is 'columnA:INTEGER,columnB:STRING'.        |
| `$RoundRules`       | `String`  | no        | `[string]::Empty` | If set, should be a comma-separated list of rounding rules. Example format is 'columnA:2dp,columnB:5mi'.                                                  |
| `$ApplyTrim`        | `Boolean` | no        | `$false`          | If set, then all string values read in are trimmed prior to further processing (TRIM LEFT and TRIM RIGHT steps).                                          |
| `$AutoDetectTypes`  | `Boolean` | no        | `$false`          | If set, then column data types are auto-detected.                                                                                                         |
| `$ScanRows`         | `Integer` | no        | `$null`           | This parameter is used to control the number of rows scanned in the left and right datasets when calculating data types (SCAN LEFT and SCAN RIGHT steps). |
| `$SummariseResults` | `Boolean` | no        | `$true`           | If set to `$true`, then summary results are produced. Otherwise detailed results are produced.                                                            |
| `$DetailedRows`     | `Integer` | no        | 0                 | This parameter is used to limit the number of detailed results output.                                                                                    |

## Reporting Issues
If you find this tool useful, and would like to report issues, please contact me.

--- end of document ---